# 项目协作规范

本规范适用于“基于大语言模型的课程思政案例库信息化平台”仓库。目标是让四名成员能够并行开发，同时保持字段、接口、配置和运行方式一致。

## 一、基本原则

- `main` 始终保持可审查、可复现的稳定状态；
- 不直接向 `main` 提交日常功能代码；
- 一个分支只处理一个相对独立的任务；
- 共享字段和接口先更新契约，再实现前后端代码；
- 不提交真实密钥、密码、Token、用户数据或运行数据；
- 所有代码注释统一使用简体中文；
- 重大架构调整、数据迁移和共享文件删除应先在组内说明影响。

## 二、分支规范

从最新的 `main` 创建短期分支，建议使用以下前缀：

```text
feature/    新功能
fix/        缺陷修复
docs/       文档修改
refactor/   不改变外部行为的重构
test/       测试补充
chore/      构建、配置和工程维护
```

示例：

```text
feature/t4-backend-skeleton
feature/t1-case-data-schema
feature/t2-evaluation-rubric
feature/t3-case-list-page
fix/case-response-validation
docs/system-architecture
```

分支应在任务完成并合并后及时停止使用，避免建立长期个人分支。

## 三、提交规范

推荐使用以下提交类型：

```text
feature: 新增功能
fix: 修复问题
docs: 修改文档
refactor: 重构代码
test: 增加或调整测试
chore: 工程、配置或依赖维护
```

示例：

```text
feature: 新增后端健康检查接口
docs: 补充案例字段对齐结论
fix: 修正评价结果缺字段时的错误处理
```

提交要求：

- 一次提交聚焦一个目的；
- 提交信息应说明实际变化，避免使用“修改一下”“更新代码”等模糊描述；
- 不提交无法运行的大量临时代码；
- 不将格式化、功能修改和无关文件混在同一提交中；
- 合并前清理调试输出和临时文件。

## 四、Pull Request

功能、修复和共享文档原则上通过 Pull Request 合并。

Pull Request 描述至少包括：

1. 本次解决的问题；
2. 主要修改内容；
3. 验证方式和结果；
4. 是否修改接口、字段、数据库或配置；
5. 是否影响其他成员；
6. 尚未解决的问题。

审查重点：

- 是否符合当前需求和模块边界；
- 是否泄露密钥或敏感数据；
- 接口和共享字段是否同步更新契约；
- 数据库变更是否包含迁移；
- 错误处理和日志是否清晰；
- 是否提供与风险相匹配的测试；
- 文档和启动说明是否需要更新。

## 五、接口与数据契约

以下内容属于共享契约：

- OpenAPI 接口定义；
- 请求和响应 JSON Schema；
- 案例、标签和评价结果字段；
- 状态枚举与错误码；
- 前端 Mock 数据；
- MaxKB 工作流输入输出。

契约发生变化时：

1. 在 `contracts/` 中修改对应文件；
2. 记录变更原因和兼容性影响；
3. 通知受影响成员；
4. 同步修改 Mock、后端实现和测试；
5. 完成回归验证后再合并。

禁止仅通过口头消息改变字段名、类型或返回结构。

## 六、配置与密钥

- 本地配置写入 `.env`，仓库只保留 `.env.example`；
- 示例值必须是明确的假值，不得使用真实密钥；
- 前端环境变量不得包含 MaxKB API Key、模型密钥或数据库密码；
- 密钥只能通过服务端环境变量或受控的部署配置提供；
- 日志不得输出密码、完整 Token、完整 API Key 或敏感用户信息；
- 新增环境变量时必须同步更新 `.env.example` 和相关文档。

## 七、代码与文档要求

- 所有代码注释统一使用简体中文；
- 变量、函数、类和接口字段使用清晰一致的英文命名；
- 避免无意义缩写和含义不清的魔法值；
- 后端路由、业务服务、数据访问和外部集成应分层组织；
- 前端不得直接依赖 MaxKB 内部管理接口；
- 文档中的命令必须经过实际验证；
- 尚未实现的内容应明确标注“计划中”，不得写成已完成功能。

## 八、测试要求

根据修改范围执行相应验证：

- 后端：单元测试、接口测试、迁移检查；
- 前端：构建检查、核心交互测试；
- 契约：Schema 校验、Mock 与真实接口一致性检查；
- MaxKB 集成：超时、空结果、缺字段和不可用场景；
- 数据：必填字段、枚举、重复编号和标签合法性检查；
- 部署：健康检查、容器重启和基础冒烟测试。

Pull Request 中应记录实际执行的测试，不能只写“已测试”。

## 九、文件与数据管理

不得提交：

- `.env` 和真实凭据；
- 数据库文件和 Docker Volume；
- 用户上传文件；
- 未经授权的大量原始资料；
- 含隐私或版权风险的数据；
- 日志、缓存、依赖目录和构建产物；
- 本地备份和临时导出文件。

确需提交样例数据时，应控制数量、完成脱敏并注明来源和用途。

## 十、问题沟通

出现以下情况时应先沟通再继续：

- 共享字段含义不明确；
- 需要改变总体架构或技术栈；
- 需要删除或重建共享数据；
- 需要修改已发布接口；
- 需要更换 MaxKB、模型或向量模型版本；
- 发现密钥泄露、数据损坏或严重安全问题。

结论应写入仓库文档或 Pull Request，避免只保留在聊天记录中。
